# 開發紀錄 — 2026-05

> 本月 active log。新 entry **直接 append 到本檔最下方**（時間正序），不維護倒序索引。
> 寫法（10 欄 template / 大事 vs 小事）見 [docs/CLAUDE.md](../CLAUDE.md#log-寫法)。
> 2026-05-31 之前的歷史在 [Log.md](../Log.md)（已封存）。

---

## 2026-05-31 — Context engineering 重構（降低 token 消耗）
Type: Refactor + Documentation
Phase: 跨階段（工作流基建）
Commit: (待 commit)

### Context
使用者反映目前的 context engineering 太耗 token，要求參考新加入的 `context-engineering-template/` 重構。診斷後確認 token 成本不在「自動載入」（只有 `CLAUDE.md` 會自動進 context），而在工作流強制觸發的讀寫：每個任務前要讀完 `plan.md`（495 行）整段 Phase；每次小改都要在 132.7K 的 `Log.md` 頂部維護倒序索引；沒有 router 導致整檔讀；`Log.md` 又是口頭報告 30% 素材不能刪。

### AI Contribution
- 讀模板（README / EXTRACTED_WORKFLOW / CLAUDE.md / scripts）抽出精華 = 資訊拓撲（retrieval-first router + 現況/歷史分離 + 大小紀律），而非照搬其 Node 工具。
- 設計並實作新拓撲：新增 `docs/index.md`（router）、`docs/CLAUDE.md`（governance：大/小改流程、衝突優先序、Log 寫法 template）。
- 精簡 `CLAUDE.md`（Source of Truth 改 retrieval-first、workflow/doc 細節改指向 governance）。
- 精簡 `plan.md`：214 行逐日進度敘事 → 12 列 phase 級表格，細節指向 log。
- 重構 log：凍結 `Log.md`（加封存橫幅，內容不動）、新增 `docs/log/YYYY-MM.md` 月度 active log（純檔底 append、無倒序索引）。
- 精簡 `STATUS.md`：移除「目前階段」巨段與「已完成」逐項敘事（與 plan 表 + log 重複），10980 → 5997 bytes。
- 適配並導入零相依 Node guard 腳本（size / links / placeholders / narrative + new-log-month + refresh orchestrator），改寫路徑清單符合本專案拓撲，丟棄模板綁 adr/tasks/state 的腳本。修掉模板 `docs-utils.walk` 的 Windows 路徑 bug。

### Human Decision / Review
使用者透過 AskUserQuestion 拍板三項：(1) Log.md 保留並封存、新 entry 改寫進 `docs/log/` 依月分檔；(2) 現況檔用「新增 `index.md` + 重用 `STATUS.md`」；(3) 導入 Node guard 腳本（先確認零相依、純 dev-only、不碰 build/demo 才執行）。實作走 plan mode 全程 → 使用者核准 plan 後才動手。

### Details
- 啟動鏈由「`CLAUDE.md` → 讀 plan.md 整段 Phase」改為「`CLAUDE.md` → `docs/index.md` → `STATUS.md` → 按意圖檢索」。
- guard-size 上限：`CLAUDE.md` 8K、`index.md` 4K、`STATUS.md` 7K、`docs/CLAUDE.md` 8K、`plan.md` 30K、每個 `docs/log/YYYY-MM.md` 45K（逼月度滾動）。
- `docs:refresh` 改用 `process.execPath` 取代 `shell:true`，消除 Node DEP0190 警告。

### Verification / Tests Performed
- `npm run docs:refresh` 全綠：size / links / placeholders / narrative 全 PASS。
- 負向測試：在 `index.md` 注入 `## 2026-01-01` dated heading → narrative-check FAIL（exit 1）；還原後 PASS（exit 0）。
- 確認 `CMakeLists.txt` 無 `file(GLOB`、原始碼逐一列舉 → `scripts/`、`package.json` 不被 C++ build 參照，demo `.exe` 不需 Node。
- 心智走查啟動鏈總量：`index.md`(1.8K) + `STATUS.md`(6.0K) 取代舊「plan.md 整段 Phase」（數十 K）。

### Result
文件拓撲重構完成並通過所有 guard。每任務「必讀小檔」總量大幅下降；每次 log append 不再觸碰 132.7K 的 `Log.md`。新增 `docs/index.md`、`docs/CLAUDE.md`、`docs/log/2026-05.md`、`package.json`、`scripts/docs-*.mjs` 6 支；精簡 `CLAUDE.md` / `docs/plan.md` / `docs/STATUS.md` / `docs/DEV_GUIDE.md`；凍結 `docs/Log.md`。

### Risks / Follow-ups
- `Log.md` 內容凍結保留（口頭報告 30% 素材完整無損），但歷史敘事現分散在「封存 Log.md + plan 表 + STATUS checklist」，寫報告時需從這三處彙整。
- `context-engineering-template/` 仍是未追蹤目錄；功能已抽取完畢，使用者可自行刪除或移出 repo。
- 6 月起的新 entry 先跑 `npm run docs:new-log-month` 開該月份的 log 檔，再 append。

### Other Notes
口試可能會問「為什麼這樣組織文件 / 怎麼控管 AI context」——這是 retrieval-first + 現況/歷史分離 + 機器可檢查的大小紀律，值得在 learning-notes 補一則。
